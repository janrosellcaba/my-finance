local run:          `npm run dev`
deploy:             `git add . && git commit -m "..." && git push`
manage local db:    `npm run studio`
manage remote db:   `npm run studio:remote`
update db schema:   `npm run db:generate`
update local db:    `npm run db:migrate:local`
update remote db:   `npm run db:migrate:remote`